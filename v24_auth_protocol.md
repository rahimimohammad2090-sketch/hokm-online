# حکم آنلاین v24 — احراز هویت

## پیام‌های جدید

### ثبت‌نام
`REGISTER`
- username
- password
- displayName

### ورود
`LOGIN`
- username
- password

### احراز نشست
`AUTH`
- token

### پاسخ
`AUTH_OK`
- token
- user

`AUTHENTICATED`
- user

## قوانین امنیتی
- رمز عبور خام ذخیره نمی‌شود.
- از `scrypt` برای مشتق‌سازی کلید استفاده شده است.
- نشست‌ها توکن تصادفی دارند و TTL دارند.
- شناسه کاربر سرور تعیین می‌کند، نه کلاینت.
- ورود به اتاق بدون احراز هویت رد می‌شود.
- کارت‌های بازیکنان همچنان خصوصی باقی می‌مانند.

## برای نسخه production
- PostgreSQL برای کاربران
- Redis برای session/rate-limit
- Argon2id در صورت امکان
- HTTPS/WSS
- ایمیل/شماره برای بازیابی حساب
- 2FA اختیاری
- لاگ امنیتی
