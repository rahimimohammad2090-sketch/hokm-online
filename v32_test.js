const assert=require('assert'),fs=require('fs'),os=require('os'),path=require('path');const{EventStore}=require('./v32_event_store'),{TurnTimer}=require('./v32_turn_timer'),{HokmGame}=require('./v30_hokm_engine'),{recover}=require('./v32_recovery');const d=fs.mkdtempSync(path.join(os.tmpdir(),'h32-')),s=new EventStore(d);s.append('m',{seq:1,type:'GAME_STARTED'});assert.equal(s.readEvents('m').length,1);const p=[0,1,2,3].map(seat=>({userId:'u'+seat,seat})),g=new HokmGame({matchId:'m',players:p,rng:()=>.2});g.start();g.setTrump(g.hokmPlayer,'H');s.saveSnapshot('m',{...g,__v32Snapshot:true});const r=recover(s,'m',p);assert.equal(r.status,'PLAYING');assert.equal(r.trump,'H');let hit=false;const t=new TurnTimer({turnMs:15,onTimeout:()=>hit=true});t.start(2);setTimeout(()=>{assert(hit);console.log('v32 tests: PASS')},30);
